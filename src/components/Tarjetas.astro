<!-- Tarjetas informativas -->
<section id="proceso" class="py-20 px-4 md:px-16">
  <div class="max-w-7xl mx-auto text-center mb-12">
    <h2 class="text-3xl md:text-4xl font-bold text-[#C8102E]">¿Por qué elegir 8Reves?</h2>
    <p class="mt-2 text-gray-700 text-lg">Creatividad, calidad y un proceso visual que conecta</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
    <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:-translate-y-2 transition-all duration-300">
      <div class="relative h-40 bg-gradient-to-r from-red-500 to-red-700 flex items-center justify-center">
        <svg class="w-14 h-14 text-white animate-bounce" fill="none" stroke="currentColor" stroke-width="2"
          viewBox="0 0 24 24"><path d="M12 4v16m8-8H4"></path></svg>
      </div>
      <img src="/public/Logotipo1.jpg" alt="Diseño" class="w-full h-48 object-cover" />
      <div class="p-6 text-center">
        <h3 class="text-xl font-semibold text-[#C8102E] mb-2">Diseño de Marca</h3>
        <p class="text-gray-700">Creamos identidades visuales que reflejan el alma de tu empresa.</p>
      </div>
    </div>

    <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:-translate-y-2 transition-all duration-300">
      <div class="relative h-40 bg-gradient-to-r from-red-500 to-red-700 flex items-center justify-center">
        <svg class="w-14 h-14 text-white animate-spin" fill="none" stroke="currentColor" stroke-width="2"
          viewBox="0 0 24 24"><path d="M12 4v2m6.364 1.636l-1.414 1.414M20 12h-2M17.364 17.364l-1.414-1.414M12 20v-2M6.636 17.364l1.414-1.414M4 12h2M6.636 6.636l1.414 1.414" /></svg>
      </div>
      <img src="/public/Logotipo1.jpg" alt="Proceso" class="w-full h-48 object-cover" />
      <div class="p-6 text-center">
        <h3 class="text-xl font-semibold text-[#C8102E] mb-2">Proceso Visual</h3>
        <p class="text-gray-700">Cada proyecto sigue un flujo claro: idea, boceto, ejecución y entrega.</p>
      </div>
    </div>

    <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:-translate-y-2 transition-all duration-300">
      <div class="relative h-40 bg-gradient-to-r from-red-500 to-red-700 flex items-center justify-center">
        <svg class="w-14 h-14 text-white animate-pulse" fill="none" stroke="currentColor" stroke-width="2"
          viewBox="0 0 24 24"><path d="M12 8v4l3 3"></path><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" /></svg>
      </div>
      <img src="/public/Logotipo1.jpg" alt="Calidad" class="w-full h-48 object-cover" />
      <div class="p-6 text-center">
        <h3 class="text-xl font-semibold text-[#C8102E] mb-2">Calidad Visual</h3>
        <p class="text-gray-700">Utilizamos materiales premium y tecnología de última generación.</p>
      </div>
    </div>
  </div>
</section>

<!-- Scripts Alpine y scroll suave -->
<script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
<script is:raw>
  function carouselData() {
    return {
      active: 0,
      images: [
        { src: '/img/carrusel1.jpg', alt: 'Proyecto 1' },
        { src: '/img/carrusel2.jpg', alt: 'Proyecto 2' },
        { src: '/img/carrusel3.jpg', alt: 'Proyecto 3' }
      ],
      next() { this.active = (this.active + 1) % this.images.length; },
      prev() { this.active = (this.active - 1 + this.images.length) % this.images.length; }
    };
  }

  document.querySelectorAll('a[href^="#"]').forEach(link => {
    link.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({ behavior: 'smooth' });
      }
    });
  });
</script>
