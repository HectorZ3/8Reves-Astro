
---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Animacion from '../components/Animacion.astro';
---

<Layout title="Inicio | 8Reves">

<Navbar/>

  <!-- Sección de animación -->

<Animacion/>

  <!-- Carrusel -->
  <section id="trabajo" class="w-full bg-white py-16 px-4 md:px-16" x-data="carouselData()">
    <div class="max-w-5xl mx-auto text-center mb-10">
      <h2 class="text-3xl font-bold text-[#C8102E]">Nuestro trabajo en acción</h2>
      <p class="text-gray-600 mt-2">Explora algunos de nuestros diseños destacados</p>
    </div>
    <div class="relative overflow-hidden">
      <div class="flex transition-transform duration-500 ease-in-out" :style="`transform: translateX(-${active * 100}%);`">
        <template x-for="(image, index) in images" :key="index">
          <div class="min-w-full flex-shrink-0">
            <img :src="image.src" :alt="image.alt" class="w-full h-72 md:h-96 object-cover rounded-xl shadow-md" />
          </div>
        </template>
      </div>
      <button @click="prev" class="absolute top-1/2 left-4 transform -translate-y-1/2 bg-[#C8102E] text-white p-3 rounded-full shadow-lg hover:bg-red-600 transition">
        &#8592;
      </button>
      <button @click="next" class="absolute top-1/2 right-4 transform -translate-y-1/2 bg-[#C8102E] text-white p-3 rounded-full shadow-lg hover:bg-red-600 transition">
        &#8594;
      </button>
    </div>
    <div class="flex justify-center gap-2 mt-6">
      <template x-for="(image, index) in images" :key="index">
        <button @click="active = index" :class="active === index ? 'bg-[#C8102E]' : 'bg-gray-300'" class="w-3 h-3 rounded-full"></button>
      </template>
    </div>
  </section>

  <!-- Tarjetas informativas -->
  <section id="proceso" class="py-20 px-4 md:px-16 bg-gray-50">
    <div class="max-w-7xl mx-auto text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-[#C8102E]">¿Por qué elegir 8Reves?</h2>
      <p class="mt-2 text-gray-700 text-lg">Creatividad, calidad y un proceso visual que conecta</p>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
      <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:-translate-y-2 transition-all duration-300">
        <div class="relative h-40 bg-gradient-to-r from-red-500 to-red-700 flex items-center justify-center">
          <svg class="w-14 h-14 text-white animate-bounce" fill="none" stroke="currentColor" stroke-width="2"
            viewBox="0 0 24 24"><path d="M12 4v16m8-8H4"></path></svg>
        </div>
        <img src="/img/branding.jpg" alt="Diseño" class="w-full h-48 object-cover" />
        <div class="p-6 text-center">
          <h3 class="text-xl font-semibold text-[#C8102E] mb-2">Diseño de Marca</h3>
          <p class="text-gray-700">Creamos identidades visuales que reflejan el alma de tu empresa.</p>
        </div>
      </div>
      <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:-translate-y-2 transition-all duration-300">
        <div class="relative h-40 bg-gradient-to-r from-red-500 to-red-700 flex items-center justify-center">
          <svg class="w-14 h-14 text-white animate-spin" fill="none" stroke="currentColor" stroke-width="2"
            viewBox="0 0 24 24"><path d="M12 4v2m6.364 1.636l-1.414 1.414M20 12h-2M17.364 17.364l-1.414-1.414M12 20v-2M6.636 17.364l1.414-1.414M4 12h2M6.636 6.636l1.414 1.414" /></svg>
        </div>
        <img src="/img/proceso.jpg" alt="Proceso" class="w-full h-48 object-cover" />
        <div class="p-6 text-center">
          <h3 class="text-xl font-semibold text-[#C8102E] mb-2">Proceso Visual</h3>
          <p class="text-gray-700">Cada proyecto sigue un flujo claro: idea, boceto, ejecución y entrega.</p>
        </div>
      </div>
      <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:-translate-y-2 transition-all duration-300">
        <div class="relative h-40 bg-gradient-to-r from-red-500 to-red-700 flex items-center justify-center">
          <svg class="w-14 h-14 text-white animate-pulse" fill="none" stroke="currentColor" stroke-width="2"
            viewBox="0 0 24 24"><path d="M12 8v4l3 3"></path><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" /></svg>
        </div>
        <img src="/img/materiales.jpg" alt="Calidad" class="w-full h-48 object-cover" />
        <div class="p-6 text-center">
          <h3 class="text-xl font-semibold text-[#C8102E] mb-2">Calidad Visual</h3>
          <p class="text-gray-700">Utilizamos materiales premium y tecnología de última generación.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Scripts Alpine y scroll suave -->
  <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
  <script is:raw>
    function carouselData() {
      return {
        active: 0,
        images: [
          { src: '/img/carrusel1.jpg', alt: 'Proyecto 1' },
          { src: '/img/carrusel2.jpg', alt: 'Proyecto 2' },
          { src: '/img/carrusel3.jpg', alt: 'Proyecto 3' }
        ],
        next() { this.active = (this.active + 1) % this.images.length; },
        prev() { this.active = (this.active - 1 + this.images.length) % this.images.length; }
      };
    }
    document.querySelectorAll('a[href^=\"#\"]').forEach(link => {
      link.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
  </script>

  <!-- Carrusel de portafolio -->
<section id="portafolio" class="py-16 px-4 md:px-16 bg-white" x-data="carouselPortafolio()">
  <div class="max-w-6xl mx-auto text-center mb-10">
    <h2 class="text-3xl font-bold text-[#C8102E]">Nuestro Portafolio</h2>
    <p class="text-gray-600 mt-2">Algunos de los logos y diseños realizados por 8Reves</p>
  </div>

  <div class="relative overflow-hidden">
    <div class="flex transition-transform duration-500 ease-in-out" :style="`transform: translateX(-${active * 100}%);`">
      <template x-for="(img, index) in images" :key="index">
        <div class="min-w-full md:min-w-1/3 p-4">
          <div class="bg-white shadow-lg rounded-xl overflow-hidden hover:scale-105 transition">
            <img :src="img.src" :alt="img.alt" class="w-full h-60 object-contain p-6" />
          </div>
        </div>
      </template>
    </div>

    <!-- Botones -->
    <button @click="prev" class="absolute top-1/2 left-4 -translate-y-1/2 bg-[#C8102E] text-white p-2 rounded-full shadow hover:bg-red-700">‹</button>
    <button @click="next" class="absolute top-1/2 right-4 -translate-y-1/2 bg-[#C8102E] text-white p-2 rounded-full shadow hover:bg-red-700">›</button>
  </div>

  <div class="flex justify-center gap-2 mt-6">
    <template x-for="(img, index) in images" :key="index">
      <button @click="active = index" :class="active === index ? 'bg-[#C8102E]' : 'bg-gray-300'" class="w-3 h-3 rounded-full"></button>
    </template>
  </div>
</section>
<script is:raw>
  function carouselPortafolio() {
    return {
      active: 0,
      images: [
        { src: '/img/logo1.png', alt: 'Logo 1' },
        { src: '/img/logo2.png', alt: 'Logo 2' },
        { src: '/img/logo3.png', alt: 'Logo 3' },
        { src: '/img/logo4.png', alt: 'Logo 4' },
        { src: '/img/logo5.png', alt: 'Logo 5' }
      ],
      next() { this.active = (this.active + 1) % this.images.length },
      prev() { this.active = (this.active - 1 + this.images.length) % this.images.length }
    };
  }
</script>


</Layout>
<!-- Footer -->
<footer class="bg-gray-800 text-white py-6">
  <div class="max-w-7xl mx-auto text-center">
    <p class="text-sm">© 2023 8Reves. Todos los derechos reservados.</p>
    <p class="text-xs mt-2">Diseñado con amor y creatividad</p>
  </div>
</footer>
